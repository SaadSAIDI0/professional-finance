# App Assets

Put your application logo here as:

```text
logo.png
```

The dashboard automatically uses `app/assets/logo.png` when the file exists.
If no logo is found, the app shows a simple `PF` badge instead.

Recommended logo size:

```text
512x512 PNG
```

